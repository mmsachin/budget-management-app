{
  "name": "budget-frontend",
  "version": "1.0.0",
  "description": "Frontend for the budget management app",
  "scripts": {
    "build": "ng build --prod --base-href=./",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "@angular/core": "^1.8.2",
    "@angular/common": "^1.8.2",    
    "angular": "^1.8.2",
    "angular-route": "^1.8.2"
  },
  "devDependencies": {
    "@angular/cli": "^1.7.4"
  }
}